{
  "name": "@civitas-cerebrum/element-interactions",
  "version": "0.1.1",
  "description": "A robust, readable interaction and assertion Facade for Playwright. Abstract away boilerplate into semantic, English-like methods, making your test automation framework cleaner, easier to maintain, and accessible to non-developers.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && tsc",
    "test": "npx playwright test",
    "test:ui": "npx playwright test --headed",
    "prepublishOnly": "npm run build",
    "postinstall": "node scripts/postinstall.js",
    "test:quiet": "TESTER_DEBUG=false npx playwright test",
    "test:coverage": "RUN_COVERAGE=1 npx playwright test tests/api-coverage.spec.ts"
  },
  "files": [
    "dist",
    "skills",
    "scripts/postinstall.js"
  ],
  "peerDependencies": {
    "@civitas-cerebrum/context-store": ">=0.0.2",
    "@civitas-cerebrum/element-repository": ">=0.0.5",
    "@civitas-cerebrum/email-client": ">=0.0.1",
    "@playwright/test": ">=1.0.0"
  },
  "devDependencies": {
    "@civitas-cerebrum/context-store": "^0.0.2",
    "@civitas-cerebrum/element-repository": "file:../pw-element-repository",
    "@civitas-cerebrum/email-client": "file:../email-client",
    "@playwright/test": "^1.58.2",
    "@types/debug": "^4.1.13",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "keywords": [
    "playwright",
    "automation",
    "interactions",
    "assertions",
    "clean-code",
    "facade-pattern",
    "test-automation",
    "steps-api"
  ],
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/civitas-cerebrum/element-interactions.git"
  },
  "author": "Umut Ay Bora",
  "license": "MIT",
  "dependencies": {
    "debug": "^4.4.3"
  }
}
